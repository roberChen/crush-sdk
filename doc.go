// Package client is a Go client for Crush's client-server mode. It
// connects to a `crush server` over a Unix socket, TCP, or a Windows
// named pipe and drives it through the same JSON-over-HTTP protocol the
// built-in TUI uses.
package client
